<%- if controller_name != 'sessions' %>
  <div class="container mt-4 offset-md-2">
    <%= link_to "こちら", new_session_path(resource_name), class: "text-center" %>　からログインしてください。
  </div>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <div class="container mt-4 offset-md-2">
    <%= link_to "こちら", new_registration_path(resource_name), class: "text-center" %>　から新規登録を行ってください。
  </div>
<% end %>