<%= form_with url: item_search_path, method: :get do |f| %>
  <%= f.select :genre_id, options_from_collection_for_select(Genre.all, :id, :name), {include_blank: @genres[0]['name'] }, { class: "col-6 col-sm-auto text-center"} %>
  <%= f.submit 'ジャンル検索', class: "btn btn-outline-dark" %>
<% end %>
