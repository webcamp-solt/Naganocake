<div class='container px-5 px-sm-0'>
  <div class='row my-3'>
    <div class="col-md-10 mx-auto">
      <div class="row">
        <h2>商品一覧</h2>
        <div class="col text-right">
          <%= link_to new_admin_item_path,class: 'btn-outline-default' do %>
           <i class="fas fa-plus-circle fa-2x"></i>
          <% end %>
        </div>
      </div>

      <table class='table'>
        <thead>
          <tr>
            <th>商品ID</th>
            <th>商品名</th>
            <th>税抜価格</th>
            <th>ジャンル</th>
            <th>ステータス</th>
          </tr>
        </thead>
        <tbody>
          <% @items.each do |item| %>
          <tr>
            <td><%= item.id %></td>
            <td><%= link_to item.name, admin_item_path(item)  %></td>
            <td><%= item.price %></td>
            <td><%= item.genre.name %></td>
            <td>
              <%if item.sale_status == true %>
                <b class="text-success">販売中</b>
              <% else %>
                <b class="text-dark">販売停止中</b>
              <% end %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>
      <%= paginate @items %>
    </div>
  </div>
</div>